CREATE TABLE TREE_EMPLOYEE (ID BIGINT NOT NULL, NAME VARCHAR(255), BOSS BIGINT NOT NULL, PRIMARY KEY (ID))
ALTER TABLE TREE_EMPLOYEE ADD CONSTRAINT TREE_EMPLOYEE_BOSS FOREIGN KEY (BOSS) REFERENCES TREE_EMPLOYEE (ID)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT DECIMAL(15), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
