CREATE TABLE CAR (ID BIGINT NOT NULL, DISC VARCHAR(31), CONSTRUCTIONDATE TIMESTAMP, MODELNAME VARCHAR(255), PRICE INTEGER, MANUFACTURER BIGINT NOT NULL, PRIMARY KEY (ID))
CREATE TABLE PERSON (ID BIGINT NOT NULL, DISC VARCHAR(31), BIRTHDATE DATE, FIRSTNAME VARCHAR(255), LASTNAME VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE CUSTOMER (ID BIGINT NOT NULL, DISCOUNT INTEGER, IBAN VARCHAR(255), ISBUYER SMALLINT DEFAULT 0, ISSELLER SMALLINT DEFAULT 0, PHONENUMBER VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE EMPLOYEE (ID BIGINT NOT NULL, EMPLOYEENUMBER BIGINT, EMPLOYEESINCE TIME, JOBDESCRIPTION VARCHAR(255), SALARY INTEGER, BOSS BIGINT NOT NULL, PRIMARY KEY (ID))
CREATE TABLE MANUFACTURER (ID BIGINT NOT NULL, NAME VARCHAR(255), CITY VARCHAR(255), DISPOSED SMALLINT DEFAULT 0, HOUSENUMBER INTEGER, STREETNAME VARCHAR(255), ZIPCODE INTEGER, PRIMARY KEY (ID))
CREATE TABLE USED_CAR (ID BIGINT NOT NULL, MILEAGE INTEGER, STATE VARCHAR(255), SELLER BIGINT NOT NULL, PRIMARY KEY (ID))
CREATE TABLE Person_ADDRESS (CITY VARCHAR(255), DISPOSED SMALLINT DEFAULT 0, HOUSENUMBER INTEGER, STREETNAME VARCHAR(255), ZIPCODE INTEGER, Person_ID BIGINT)
ALTER TABLE CAR ADD CONSTRAINT CAR_MANUFACTURER FOREIGN KEY (MANUFACTURER) REFERENCES MANUFACTURER (ID)
ALTER TABLE CUSTOMER ADD CONSTRAINT FK_CUSTOMER_ID FOREIGN KEY (ID) REFERENCES PERSON (ID)
ALTER TABLE EMPLOYEE ADD CONSTRAINT FK_EMPLOYEE_BOSS FOREIGN KEY (BOSS) REFERENCES PERSON (ID)
ALTER TABLE EMPLOYEE ADD CONSTRAINT FK_EMPLOYEE_ID FOREIGN KEY (ID) REFERENCES PERSON (ID)
ALTER TABLE USED_CAR ADD CONSTRAINT FK_USED_CAR_ID FOREIGN KEY (ID) REFERENCES CAR (ID)
ALTER TABLE USED_CAR ADD CONSTRAINT FK_USED_CAR_SELLER FOREIGN KEY (SELLER) REFERENCES PERSON (ID)
ALTER TABLE Person_ADDRESS ADD CONSTRAINT PrsnADDRESSPrsonID FOREIGN KEY (Person_ID) REFERENCES PERSON (ID)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT DECIMAL(15), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
