CREATE TABLE BASEBYCLASS (ID BIGINT NOT NULL, DISC VARCHAR(31), VERSION INTEGER, NAME VARCHAR(255), STUFF BIGINT, PRIMARY KEY (ID))
CREATE TABLE BASE_BY_SUBCLASS (ID BIGINT NOT NULL, DISC VARCHAR(31), VERSION INTEGER, PRIMARY KEY (ID))
CREATE TABLE ONE_BY_SUBCLASS (ID BIGINT NOT NULL, NAME VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE TWO_BY_SUBCLASS (ID BIGINT NOT NULL, STUFF BIGINT, PRIMARY KEY (ID))
ALTER TABLE ONE_BY_SUBCLASS ADD CONSTRAINT ONE_BY_SUBCLASS_ID FOREIGN KEY (ID) REFERENCES BASE_BY_SUBCLASS (ID)
ALTER TABLE TWO_BY_SUBCLASS ADD CONSTRAINT TWO_BY_SUBCLASS_ID FOREIGN KEY (ID) REFERENCES BASE_BY_SUBCLASS (ID)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT DECIMAL(15), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
